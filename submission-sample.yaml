# Coldline - Task 1.6
# File: submission-sample.yaml
# Fictional shape example only; values do not answer the supplied case.
answers:
  decision_evidence_mappings:
    D01:
      # Choose one: supported, contradicted, insufficient-evidence.
      assessment: "insufficient-evidence"
      # Choose 2 unique selections; order does not matter.
      # Allowed values: E61, E62, E63, E64, E65, E66.
      evidence_ids: ["E66", "E61"]
    D02:
      # Choose one: supported, contradicted, insufficient-evidence.
      assessment: "insufficient-evidence"
      # Choose 2 unique selections; order does not matter.
      # Allowed values: E61, E62, E63, E64, E65, E66.
      evidence_ids: ["E66", "E61"]
    D03:
      # Choose one: supported, contradicted, insufficient-evidence.
      assessment: "insufficient-evidence"
      # Choose 1 unique selections; order does not matter.
      # Allowed values: E61, E62, E63, E64, E65, E66.
      evidence_ids: ["E66"]
    D04:
      # Choose one: supported, contradicted, insufficient-evidence.
      assessment: "insufficient-evidence"
      # Choose 1 unique selections; order does not matter.
      # Allowed values: E61, E62, E63, E64, E65, E66.
      evidence_ids: ["E66"]
  review_cases:
    R01:
      # Choose one: valid, invalid, needs-more-evidence.
      verdict: "needs-more-evidence"
      # Choose one: retained-entries-are-not-outstanding, marginal-percentiles-are-not-additive, acceptance-is-not-completion, assumptions-and-measurements-separated, local-results-prove-production.
      reason: "local-results-prove-production"
      # Choose 1 unique selections; order does not matter.
      # Allowed values: E61, E62, E63, E64, E65, E66.
      evidence_ids: ["E66"]
    R02:
      # Choose one: valid, invalid, needs-more-evidence.
      verdict: "needs-more-evidence"
      # Choose one: retained-entries-are-not-outstanding, marginal-percentiles-are-not-additive, acceptance-is-not-completion, assumptions-and-measurements-separated, local-results-prove-production.
      reason: "local-results-prove-production"
      # Choose 1 unique selections; order does not matter.
      # Allowed values: E61, E62, E63, E64, E65, E66.
      evidence_ids: ["E66"]
    R03:
      # Choose one: valid, invalid, needs-more-evidence.
      verdict: "needs-more-evidence"
      # Choose one: retained-entries-are-not-outstanding, marginal-percentiles-are-not-additive, acceptance-is-not-completion, assumptions-and-measurements-separated, local-results-prove-production.
      reason: "local-results-prove-production"
      # Choose 1 unique selections; order does not matter.
      # Allowed values: E61, E62, E63, E64, E65, E66.
      evidence_ids: ["E66"]
    R04:
      # Choose one: valid, invalid, needs-more-evidence.
      verdict: "needs-more-evidence"
      # Choose one: retained-entries-are-not-outstanding, marginal-percentiles-are-not-additive, acceptance-is-not-completion, assumptions-and-measurements-separated, local-results-prove-production.
      reason: "local-results-prove-production"
      # Choose 1 unique selections; order does not matter.
      # Allowed values: E61, E62, E63, E64, E65, E66.
      evidence_ids: ["E66"]
  limitations_statement:
    # Choose 3 unique selections; order does not matter.
    # Allowed values: production-capacity-unproven, managed-service-behavior-unproven, model-quality-unproven, all-production-behavior-proven.
    classifications: ["managed-service-behavior-unproven", "model-quality-unproven", "all-production-behavior-proven"]
    # Choose 1 unique selections; order does not matter.
    # Allowed values: E61, E62, E63, E64, E65, E66.
    evidence_ids: ["E66"]
  next_measurement:
    # Choose one: two-worker-completions-queue-age-db-p95-duplicates, add-component-p95-values, multi-region-deploy-without-pilot.
    measurement: "multi-region-deploy-without-pilot"
    # Choose one: test-scaling-contention-and-duplicate-risk, prove-production-availability.
    purpose: "prove-production-availability"
    # Choose 1 unique selections; order does not matter.
    # Allowed values: E61, E62, E63, E64, E65, E66.
    evidence_ids: ["E66"]
