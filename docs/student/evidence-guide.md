# Task 1.6 supplied synthesis case

Use [evidence-pack.json](evidence-pack.json) to map D01-D04 to bounded evidence selections and assess R01-R04 review cases. This fixed public case replaces automatic grading of self-attestations and unbounded prose. It is independent of the existing single protected held-out runtime scenario.

For each claim classify support and select the minimal directly applicable evidence set. D01 requires both runtime topology and measured load evidence; D02 requires both the planning model and its fidelity limit. For each review case select a verdict, a reason and the directly applicable source record. A claim that local tests prove production behavior has insufficient evidence; that does not demonstrate that production behavior must fail.

Record exactly three limitations and choose the policy-aligned next measurement with its purpose and source. All selections are bounded by the public schema. Evidence arrays are order independent and reject duplicates. The public checker validates structure; protected assessment checks every supplied answer field.

Keep your own [decision-evidence record](decision-evidence-record.md) for the instructor's final defense. Its prose and your presentation are not graded by this deterministic answer key. Your required live investigations remain evidence for that conversation; do not replace the fixed-case observations with your own timings in the YAML.

Select the exact number shown beside each field in the answer sheet. D01 and D02 each combine two records. D03, D04, each review case, the limitation, and the next measurement each require one directly applicable evidence record. Extra citations are not part of these bounded matching answers.
